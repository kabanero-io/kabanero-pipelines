apiVersion: v1
kind: PersistentVolume
metadata:
  name: dev-manual-pipeline-run-pvc
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  hostPath:
    path: /var/lib/dev-manual-pipeline-run-pvc

