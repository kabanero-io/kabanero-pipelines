apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: java-spring-boot2-deploy-pipeline
spec:
  resources:
    - name: docker-image
      type: image
  tasks:
    - name: deploy-task
      taskRef:
        name: java-spring-boot2-deploy-task
      resources:
        inputs:
        - name: docker-image
          resource: docker-image
